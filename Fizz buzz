class Solution:
    def fizzBuzz(self, n: int) -> List[str]:
        s = []
        print(n)
        for x in range (0, n):
            if (x+1)%3 == 0 and (x+1)%5== 0:
                s.append("FizzBuzz")
                continue
            elif (x+1)%3 == 0:
                s.append("Fizz")
                continue
            elif (x+1)%5 == 0:
                s.append("Buzz")
                continue 
            
            s.append(str(x+1))
        return s
        
